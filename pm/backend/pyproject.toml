[project]
name = "pm-backend"
version = "0.1.0"
description = "FastAPI backend for the PM MVP"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.116.1",
  "httpx>=0.28.1",
  "uvicorn[standard]>=0.35.0",
]

[dependency-groups]
dev = [
  "pytest>=8.3.2",
]

[tool.uv]
package = false
